/**
 * @file The off-site addresses this product links to, in one place.
 *
 * Two surfaces name the repository now — the footer and the star badge in the
 * header — and a renamed repo that only half of them heard about is a 404 on
 * the page that claims to verify links for a living.
 */

/**
 * Every sibling property lives at github.com/nicklaunches/<domain>, so this
 * follows the same convention.
 */
export const REPO_URL = "https://github.com/nicklaunches/builders-backlinks.com";

/** The GitHub API path for {@link REPO_URL}, for the star count. */
export const REPO_API_URL = "https://api.github.com/repos/nicklaunches/builders-backlinks.com";

export const PARENT_URL = "https://nicklaunches.com";
export const X_URL = "https://x.com/nicklaunches";
